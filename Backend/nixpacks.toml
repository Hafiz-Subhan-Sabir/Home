# Railway / Nixpacks: root directory must be Backend (contains this file + requirements.txt).
[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]
